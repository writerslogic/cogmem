# cogmem runtime dependencies
cryptography>=42.0   # did:key / VC signatures / transparency log
fastembed>=0.3.0     # local embeddings for semantic recall
cbor2>=5.4.0         # COSE_Sign1 / CBOR for SCITT signed statements
